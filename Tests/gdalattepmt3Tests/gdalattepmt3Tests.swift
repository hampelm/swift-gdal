import XCTest
@testable import gdalattepmt3

final class gdalattepmt3Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(gdalattepmt3().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
